package com.fastcampusmall.presentation.navigation

object DeepLink {
    private const val SCHEME = "fastcampus://"

    const val CATEGORY_DETAIL = "${SCHEME}category"
    const val PRODUCT_DETAIL = "${SCHEME}product"
}